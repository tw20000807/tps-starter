name: Dump ranking

on:
  workflow_dispatch:
    inputs:
      url:
        description: 'URL to ranking'

jobs:
  run:
    uses: tw20000807/tps-actions/.github/workflows/dump-ranking.yml@main
    with:
      url: ${{ inputs.url }}
