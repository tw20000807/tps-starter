name: Build tests

on:
  push:
    branches:
      - '**'
    paths:
      - '*/gen/**'
      - '*/solution/**'
      - '*/validator/**'
      - '*/problem.json'
      - '*/solutions.json'
      - '*/subtasks.json'
  workflow_dispatch:

jobs:
  run:
    uses: tw20000807/tps-actions/.github/workflows/build-tests.yml@main
