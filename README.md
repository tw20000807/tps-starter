go setting -> actions/general -> workflow permissions 

choose "Read repository contents and packages permissions" and mark "Allow GitHub Actions to create and approve pull requests"

go action and run template_initialization action first